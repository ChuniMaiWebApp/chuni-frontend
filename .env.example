# =============================================================================
# ChunithmWebApp frontend - environment template
# Copy to .env and adjust:  cp .env.example .env
#
# Nuxt maps NUXT_PUBLIC_* onto runtimeConfig.public.* automatically.
# =============================================================================

# Base URL of the NestJS API (see backend/.env -> PORT + API_PREFIX)
NUXT_PUBLIC_API_BASE=http://localhost:3333/api/v1
